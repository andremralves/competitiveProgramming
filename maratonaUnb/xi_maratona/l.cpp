#include <bits/stdc++.h>

using namespace std;

int main() {
  std::ios::sync_with_stdio(false);
  std::cin.tie(nullptr);

  int N, Q;
  cin>>N>>Q;

  for(int i=0; i<n; i++) {
    cin>>a[i];
  }

  return 0;
}
