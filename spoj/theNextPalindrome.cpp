#include <bits/stdc++.h>
#include <string>

using namespace std;

void solve(int num) {
}

int main (int argc, char *argv[])
{
    int t;
    cin >> t;
    for (int i = 0; i < t; i++) {
        int num;
        cin >> num;
        solve(num);
    }
    return 0;
}
