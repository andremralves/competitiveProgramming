/**
 *    author: mralves 
 *    created: 01-02-2023 23:20:36       
**/
#include <bits/stdc++.h>

#define pb(x) push_back(x)

using namespace std;
using ll = int64_t;

void solve() {
    int n;
    cin>>n;
    vector<int> a(n);
    vector<int> us(n, 0);
    for(int i=0; i<n; i++) cin>>a[i];
    sort(a.begin(), a.end());
}

int main ()
{
    int t;
    cin>>t;
    while (t--)
    {
        solve();    
    }
    return 0;
}
