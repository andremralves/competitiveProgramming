#include <bits/stdc++.h>

using namespace std;

int main (int argc, char *argv[])
{
    int input;
    while (cin >> input, input != 42)
    {
        std::cout << input << std::endl; 
    }     
    return 0;
}
